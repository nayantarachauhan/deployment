name: CI-CD pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    name: Deploy
    environment: pre-production
    runs-on: self-hosted
    

    steps:
    - name: checkout
      uses: actions/checkout@v1

    - name: Configure Docker credentials
      uses: docker-actions/configure-docker-credentials@v1
      with:
        docker_username: nayantara52
        docker_password: Vkssann@2805

    - name: Build and push the docker image
      uses: docker-actions/build-push@v2
      with:
        context: .
        push: true
        tags: nayantara52/nayan-docker-images:version1

    - name: Login/deploy to Amazon EC2
      uses: aws-actions/amazon-ec2-login@v1
      with:
        host: ec2-3-17-56-142.us-east-2.compute.amazonaws.com
        username: ec2-user
        key: q212HLOhOUHMu84nReBwd2Fote9huRhDvvXCf24G
        script: |
          docker stop nayan-docker-images || true
          docker rm nayan-docker-images || true
          docker pull nayantara52/nayan-docker-images:version1
          docker run -d --name webapp -p 80:80 nayantara52/nayan-docker-images:version1


    

  


